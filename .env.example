# Your Discord bot token (Developer Portal -> your app -> Bot -> Reset Token)
DISCORD_TOKEN=

# Optional: a guild (server) ID to register slash commands to instantly during development.
# Leave blank to register commands globally (can take up to ~1 hour to propagate).
DISCORD_DEV_GUILD=

# Optional: set to "production" on your server to reduce log verbosity.
NODE_ENV=development
